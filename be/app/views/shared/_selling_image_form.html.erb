
<%= form_for(@selling_image) do |f| %>
  <p>Upload Image</p>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Compose new selling_image..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>
