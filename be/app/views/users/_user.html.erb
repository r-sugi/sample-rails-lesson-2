<li>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= button_to "delete", user, method: :delete,
                                  data: { confirm: "You sure?" }, form: {style: 'display:inline-block;'} %>
  <% end %>
</li>
